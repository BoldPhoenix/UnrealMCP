"""Vendored third-party code, kept verbatim (not our own work).

remote_execution.py is copied UNCHANGED from the engine install's PythonScriptPlugin so the
protocol version is pinned to the exact UE build we run. Re-sync it if the engine version changes.
"""
